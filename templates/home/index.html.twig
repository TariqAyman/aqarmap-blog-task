{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-md-8 col-lg-8 col-xl-8">
        <div class="section-title">
            <h2 class="title">Recent posts</h2>
        </div>

        {% for article in articles %}
            <!-- post -->
            {{ include('article/preview.article.html.twig', {'article': article}) }}
            <!-- /post -->
             <hr class="my-4"/>
        {% endfor %}

        <div class="d-flex justify-content-end mb-4">
            <a class="btn btn-primary text-uppercase" href="{{ url('article_index') }}">Older Posts →</a>
        </div>
    </div>
    <div class="col-lg-4">
        <div>
            <ul>
                {% for category in categories %}
                    <li>{{ include('category/category.html.twig', {'category': category}) }}</li>
                {% endfor %}
            </ul>
            <div class="section-row text-center">
                <a href="{{ url('category_index') }}" class="primary-button">See all categories</a>
            </div>
        </div>
    </div>

{% endblock %}
